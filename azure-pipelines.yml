trigger:
- main

pool:
  name: 'Default'

steps:
- checkout: self
  displayName: "Récupération du code GitHub"

# Étape d'exécution qui utilise le secret de manière sécurisée (script au singulier)
- script: |
    echo === Connexion securisee a la base de donnees ===
    echo Nous allons simuler une action sur Oracle.
    
    # Le système va masquer automatiquement la variable avec des étoiles '***'
    echo "Le mot de passe utilise en memoire est : $(ORACLE_PASSWORD)"
    
    echo === Fin de l'action ===
  displayName: "Test du coffre-fort de secrets"
  env:
    ORACLE_PASSWORD: $(ORACLE_PASSWORD)
